<?php

	// Trimmed to the settings the ruTorrent PHP transform touches; the real
	// configuration carries hundreds more that it leaves alone.
	$forbidUserSettings = false;

	$scgi_host = "127.0.0.1";
	$scgi_port = 5000;
