diff --git a/php/getplugins.php b/php/getplugins.php
index b5c916f..3f5c3f3 100644
--- a/php/getplugins.php
+++ b/php/getplugins.php
@@
-					case "plugin.version":
-					case "plugin.runlevel":
-					{
-						$info[$field] = floatval($value);
-						break;
-					}
+					case "plugin.version":
+					{
+						// Keep plugin version as-is (e.g. 5.10.1) instead of truncating via floatval.
+						$info[$field] = $value;
+						break;
+					}
+					case "plugin.runlevel":
+					{
+						$info[$field] = floatval($value);
+						break;
+					}
